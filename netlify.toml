# Sitio estatico: los archivos estan en la raiz del repo
[build]
  publish = "."

# Cabeceras basicas de seguridad
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
